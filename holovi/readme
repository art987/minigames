'''

# 保存文件
with open('/mnt/kimi/output/holographic_player.html', 'w', encoding='utf-8') as f:
    f.write(html_content)

print("✅ 全息视频播放器已创建完成！")
print("\n文件位置: /mnt/kimi/output/holographic_player.html")
print("\n🎬 功能特性：")
print("  • 沉浸式全屏播放 - 点击视频自动进入全屏模式")
print("  • 智能控制栏 - 鼠标移动显示，3秒无操作自动隐藏")
print("  • 自动连播 - 视频结束自动播放下一个")
print("  • 循环播放 - 支持单个视频循环")
print("  • 分类浏览 - 全息/3D/VR/光场/深度视觉")
print("  • 键盘快捷键 - 空格播放/暂停，F全屏，L循环，N/P切换")
print("  • 进度条拖拽 - 精确跳转")
print("  • 快进快退 - 左右方向键±10秒")






 我来为你创建一个专门的全息视频播放器网页。这个设计会专注于沉浸式的播放体验，包含清晰的分类展示和极致的全屏播放功能。
我已经为你创建了一个专业的**全息视频播放器网页工具**，文件已保存，你可以直接下载使用。

## 🎬 核心播放体验设计

**极致沉浸的播放模式：**
- **自动全屏**：点击视频后自动进入浏览器全屏，没有任何干扰
- **智能UI隐藏**：播放时控制栏完全隐藏，鼠标移动才显示，3秒无操作再次隐藏
- **无缝切换**：视频播放结束自动播放下一个，无需手动操作
- **循环播放**：支持单个视频循环，点击循环按钮或按 `L` 键切换

## ⌨️ 便捷操作方式

| 快捷键 | 功能 |
|--------|------|
| `Space` | 播放/暂停 |
| `←` `→` | 快退/快进 10秒 |
| `F` | 切换全屏 |
| `L` | 切换循环播放 |
| `N` / `P` | 下一个 / 上一个 |
| `ESC` | 退出播放 |

## 📂 如何使用

1. **替换视频**：在代码中找到 `videoData` 数组，将 `src` 替换为你的全息视频文件路径（本地或URL）
2. **替换封面**：将 `thumbnail` 替换为对应视频的封面图
3. **分类管理**：支持全息投影、3D立体、VR全景、光场、深度视觉等分类

## ✨ 视觉特色

- 赛博朋克风格界面，霓虹渐变色彩
- 卡片悬停光效和3D抬升动画
- 毛玻璃效果和背景动态光晕
- 响应式设计，支持手机/平板/电脑

这个播放器专门为长时间观看体验优化，播放时屏幕上只有纯粹的视频内容，让用户完全沉浸在全息影像中。